-- Файлы .sql / .sh в этой папке выполняются один раз, только при первом старте
-- контейнера postgres на ПУСТОМ volume (docker-entrypoint-initdb.d).
-- Если volume уже существует (например, после предыдущего запуска) — не выполнятся.
--
-- Пример: включить расширение для поиска по подстроке, пригодится для каталога семян.
-- CREATE EXTENSION IF NOT EXISTS pg_trgm;

SELECT 1;
